from setuptools import setup, find_packages
import os
"""

"""
def read(fname):
    return open(os.path.join(os.path.dirname(__file__), fname)).read()

setup(
    name="Sky Sakana",
    version="0.1",
    author = "Skyler Granatir",
    description = ("Sakana Problem Set"),
    packages=["sky_src", "neat_src1", "training_scripts"]#,
    # long_description=read('README.md')
)